## 1. 修复有序列表 CSS

- [x] 1.1 使用 `::before` + `counter()` 实现序号显示
- [x] 1.2 `list-style-type: none` 移除默认序号

## 2. 修复代码块显示

- [x] 2.1 将样式应用于内容区域而非整个 FencedCode 节点
- [x] 2.2 确保围栏标记隐藏，内容区域显示为整体框

## 3. 验证

- [x] 3.1 构建并验证